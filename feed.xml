<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://ljchen.ca/feed.xml" rel="self" type="application/atom+xml" /><link href="https://ljchen.ca/" rel="alternate" type="text/html" /><updated>2025-09-04T18:40:19+00:00</updated><id>https://ljchen.ca/feed.xml</id><title type="html">Homepage</title><subtitle>personal description</subtitle><author><name>Liangjie (Jeffrey) Chen</name></author><entry><title type="html">From geometric series to fixed point iterations and their applications: Part II</title><link href="https://ljchen.ca/blurbs/2025/08/geo-series-fixed%20point-2/" rel="alternate" type="text/html" title="From geometric series to fixed point iterations and their applications: Part II" /><published>2025-08-22T00:00:00+00:00</published><updated>2025-08-22T00:00:00+00:00</updated><id>https://ljchen.ca/blurbs/2025/08/geo-series-fixed-point-2</id><content type="html" xml:base="https://ljchen.ca/blurbs/2025/08/geo-series-fixed%20point-2/"><![CDATA[<p>Part II of a three-part note on how the common existence and uniqueness results in ODEs and MDPs boil down to the Banach fixed point theorem, which itself is based on the simple geometric series.</p>

<p><em>Acknowledgement: the content of this post, in particular the Banach fixed point theorem and its proof is based on Prof. Keith Conrad’s <a href="https://kconrad.math.uconn.edu/blurbs/analysis/contraction.pdf">note</a>.</em></p>

<p>In Part I of this three-part note, we saw how the geometric series is extended beyond a series of real or complex numbers, into operators and matrices. We introduced the Neumann series, and an application of matrix inverse computation using these tools. In this note, we look at general iterative algorithms and the famed Banach fixed point theorem, and understand how convergence to optimum may be connected to fixed points.</p>

<h2 id="iterative-algorithms-and-fixed-points">Iterative algorithms and fixed points</h2>
<p>We consider a sequence of points, say \(\{x_n\}_{n=0}^{\infty} \subseteq \mathbb{R}^n\), whose elements are recursively defined by</p>

\[\begin{equation}\label{eq:iterations}
    x_{n+1} \define T(x_{n}), \quad n \inZ_{\geq 0},
\end{equation}\]

<p>where \(T : \mathbb{R}^n \goesto \mathbb{R}^n\) is some fixed function. We may think of the elements in this sequence as <em>iterations</em> of applying the function \(T\), since unrolling the recursion we would obtain \(x_{n} = T^{n}(x_0)\) for all \(n \inZ_{\geq 0}\). Common examples of this recursion scheme include (1) the Newton’s method to find a root of some differentiable function \(f : \mathbb{R} \goesto \mathbb{R}\), which defines the iterations as</p>

\[\begin{equation}\label{eq:newton}
    x_{n+1} = x_{n} - \frac{f(x_{n})}{f'(x_{n})},  \quad n \inZ_{\geq 0},
\end{equation}\]

<p>with \(x_0\) being the initial guess of the root, 
and (2) the gradient descent algorithm to find a local minimum on some differentiable objective function \(f : \mathbb{R}^n \goesto \mathbb{R}\), which defines the iterations as</p>

\[\begin{equation}\label{eq:grad-descent}
    x_{n+1} = x_{n} - \eta \nabla f(x_{n}),  \quad n \inZ_{\geq 0},
\end{equation}\]

<p>where \(\eta\) is the step size, and \(\nabla f\) is the gradient of \(f\). 
All such iterative algorithms aims to stop at some desired point, which depending on the purpose, can be root of a function (as in Newton’s method) or local minimum (as in gradient descent), or even global minimum/maximum. One common definition that can tie these points together, is the notion of <em>fixed point</em>. For our purposes, given a function \(T : V \goesto V\) with \(V\) being a set, \(x \in V\) is a fixed point of \(T\) if \(T(x) = x\). Thus, we can interpret the Newton step \eqref{eq:newton} by defining the map</p>

\[T(x) = x - \frac{f(x)}{f'(x)}.\]

<p>Since \(x^{\star}\) is a root of \(f\) if and only if \(f(x^{\star}) = 0\), it amounts to the Newton step \eqref{eq:newton} reaching the point \(x_{n+1} = T(x_n) = x_{n}\), that is, a fixed point of \(T\). Similarly, we can define</p>

\[T(x) = x - \eta \nabla f(x)\]

<p>for gradient descent. A local minimum is an \(x^{\star}\) that satisfies \(\nabla f(x^{\star}) = \zero\), so once again it means that the gradient descent step has reached the point \(x_{n+1} = T(x_n) = x_n\), which is again a fixed point of \(T\).</p>

<p>To visualize a fixed point of some function (if one exists) on the two-dimensional Euclidean space, we simply plot \(x\) against \(T(x)\), then its fixed points are all points that intersect with the line \(y = x\). Of course, not all functions have a fixed point, e.g., the real-valued function \(T(x) = x+1\) does not have a fixed point, the constant function \(T(x) = 1\) does not have a fixed point on the interval \([0,1)\), and a fixed point is not even defined for any matrixed-valued function \(T : \mathbb{R} \goesto \mathbb{R}^{m\times n}\). Generally, there are many results that provide sufficient conditions for a fixed point to exist, though not every one of them will tell us where it is. The most prominent example is the <a href="https://en.wikipedia.org/wiki/Brouwer_fixed point_theorem">Brouwer fixed point theorem</a>, which says a continuous function mapping a compact, convex set to itself always has at least one fixed point; the generalization of this theorem to set-valued maps, called the <a href="https://en.wikipedia.org/wiki/Kakutani_fixed point_theorem">Kakutani fixed point theorem</a>, lays the foundation for the existence of a Nash equilibrium in game theory.</p>

<h2 id="banach-fixed-point-theorem-and-its-proof">Banach fixed point theorem and its proof</h2>
<p>The fixed point theorem that we focus on in this note is called the <em>Banach fixed point theorem</em>, also called the <em>contractive mapping theorem</em>. Unlike Brouwer, which provides sufficient conditions for the <em>existence</em> of a fixed point, the Banach fixed point theorem gives us conditions for when a <em>unique</em> fixed point exists, and how to find it. The latter part is particularly useful in applied scenarios, since we wouldn’t be able to do much with only the knowledge that a fixed point exists but not where it is.</p>

<p>Formally, let us consider a complete metric space \((M,d)\), and we want to answer the following questions about a function \(T : M \goesto M\), in the following order:</p>

<ol>
  <li>When does \(T\) have a fixed point?</li>
  <li>When is a fixed point \(x^{\star}\) of \(T\) unique?</li>
  <li>How to compute/find the unique fixed point \(x^{\star}\)?</li>
</ol>

<p>The answers to these questions lie in the <em>Banach fixed point theorem</em> stated below.</p>

<p><strong>Theorem</strong> (Banach fixed point theorem). <em>Suppose that there exists some positive constant \(c &lt; 1\) such that, for every \(\,x, y \in M\), the map \(\,T\) satisfies</em></p>

\[\begin{equation}\label{eq:contraction}
    d(T(x), T(y)) \leq c d(x,y).
\end{equation}\]

<p><em>Then, there exists a unique fixed point \(\,x^{\star}\) of \(\,T\) in \(\,M\), and every sequence \(\{x_n\}_{n \inZ_{\geq 0}}\) such that \(\,x_n \in M\) and \(x</em>{n+1} \define T(x_n)\) converges to \(x^{\star}\)._</p>

<p><em>Proof</em>. First, we show that if \(T\) has a fixed point, then it must be unique. Assume towards contradiction that both \(x, \tilde{x}\in M\) are both fixed points of \(T\), but \(x \neq \tilde{x}\). Then, since \(x = T(x)\)  and \(\tilde{x} = T(\tilde{x})\), we must have</p>

\[d(x, \tilde{x}) = d(T(x), T(\tilde{x})) \leq cd(x, \tilde{x}) &lt; d(x, \tilde{x}),\]

<p>which is a contradiction. Next, we show that the sequence \(\{x_n\}_{n \inZ_{\geq 0}}\) is Cauchy; in particular, since \(M\) is complete, it converges to a point in \(M\). By construction, we have</p>

\[d(x_n, x_{n+1}) \leq cd(x_{n-1}, x_n) \leq c^2d(x_{n-2}, x_{n-1}) \leq \cdots \leq c^nd(x_0, x_1)\]

<p>for \(n \in \{1,2,…\}\). For any \(m \inZ_{\geq 0}\) such that \(m &gt; n\), since \(d\) is a metric and must satisfy the triangle inequality, by the geometric series formula we also have</p>

\[\begin{align*}
    d(x_n, x_m) &amp;\leq d(x_n, x_{n+1}) + \cdots + d(x_{m-1}, x_m) \leq c^n d(x_0, x_1) + \cdots + c^{m-1}d(x_0, x_1) \\
                &amp;= \sum_{i=n}^{m-1} c^i d(x_0, x_1)  \leq \left(\sum_{i=0}^{\infty} c^i - \sum_{i=0}^{n-1} c^i\right) d(x_0, x_1) \\
                &amp;= \left(\frac{1}{1-c} - \frac{1-c^n}{1-c}\right) d(x_0, x_1) \\
                &amp;= \frac{c^n}{1-c} d(x_0, x_1).
\end{align*}\]

<p>Since \(c &lt; 1\), for any \(\epsilon &gt; 0\), there is an integer \(N &gt; 0\) such that</p>

\[\frac{c^N}{1-c} d(x_0, x_1) &lt; \epsilon.\]

<p>Thus, \(d(x_n, x_m) &lt; \epsilon\) for all \(n,m &gt; N\), so the sequence \({x_n}\) is Cauchy. Finally, let \(\bar{x} \define \lim_{n \goesto \infty} x_n\), and all we need to do is to prove that \(T(\bar{x}) = \bar{x}\) is that \(T\) is continuous, since by continuity, we can then conclude that \(T(\bar{x}) = \lim_{n \goesto \infty} T(x_n) = \lim_{n\goesto \infty} x_{n+1} = \bar{x}\). To see this, take any \(\epsilon &gt; 0\) and set \(\delta \define \frac{\epsilon}{c}\), then for any \(x, y \in M\), by \eqref{eq:contraction} we get that</p>

\[d(x, y) &lt; \delta \implies d(T(x), T(y)) \leq cd(x,y) &lt; c\delta = \epsilon,\]

<p>i.e., \(T\) is not only continuous but uniformly continuous, and the proof is complete. \(\endproof\)</p>

<p>As we can see, while the Banach fixed point theorem relies on certain “niceness” properties of the metric space and knowledge of \(\epsilon\)-\(\delta\) definition of continuity, at its core the proof just utilizes the geometric series identity to obtain the final bound on the iterations, and thereby the fixed point itself.</p>

<p>We make two additional remarks about the Banach fixed point theorem. First, any map \(T\) that satisfies \eqref{eq:contraction} is called a <em>contraction</em> (thus the alternative name of the theorem, “contraction mapping theorem”), and the constant \(c\) is called the <em>contraction constant</em>. A contraction is by definition a Lipschitz continuous function with Lipschitz constant strictly less than 1. Second, the requirement that \(T\) maps a complete metric space \(M\) to itself can be restricted to a closed invariant subset of \(M\) under \(T\). That is, let \(T\) be a contraction on \(M\), and suppose that \(\mathcal{S} \subseteq M\) is a closed set with respect to the metric topology such that \(T(\mathcal{S}) \subseteq \mathcal{S}\), then the map \(T\vert_{\mathcal{S}}: \mathcal{S} \goesto \mathcal{S}\) is a contraction since \(\mathcal{S}\) contains all of its limit points, and thus, the fixed point \(x^{\star}\) as well.</p>

<p>Finally, for practical reasons, we focus on the case of (1) Euclidean spaces, in which the \(L_p\) norm is a metric, and (2) \(T\) has additional smoothness properties such as continuous differentiability. The (induced) \(L_p\) norm of \(T\)’s Jacobian evaluated at some point \(x \inR^n\), denoted by \(\Vert J(x)\Vert \), can sometimes provide a more easily checkable sufficient condition for contractivity. The reason why this condition can be more practical than verifying \eqref{eq:contraction} point-wise is that, in most algorithmic convergence analysis from a fixed point perspective, we often know exactly what the iteration mapping \(T\) is, and if it is differentiable, then we can easily compute and bound the \(L_p\) norm of its Jacobian.</p>

<p><strong>Theorem</strong>. <em>Let \(\,\mathcal{D} \subseteq \mathbb{R}^n\) be a \(\,T\)-invariant convex set. Suppose that there exists some positive constant \(\,c &lt; 1\) such that \(\Vert J(x)\Vert \leq c \) for every \(\,\,x \in \mathcal{D}\), then \(\,T\) is a contraction with contraction constant \(\,c\), with respect to the norm \(\,\Vert \cdot \Vert\), i.e., for all \(\,x, y \in \mathcal{D}\),</em></p>

\[\Vert T(x) - T(y) \Vert \leq c \Vert x - y \Vert.\]

<p><em>Proof</em>. Take any two points \(x, y \in \mathcal{D}\), and define the line segment \(\Phi(t) \define tx + (1-t)y \) parameterized by \(t \in [0,1]\), i.e., \(\Phi(0) = y\) and \(\Phi(1) = x\). Since \(\mathcal{D}\) is convex and \(T\)-invariant, every point on the line segment, and itself under the map \(T\), must be contained in \(\mathcal{D}\). By chain rule,</p>

\[\frac{\d}{\d t} T(\Phi(t)) = J(\Phi(t)) \nabla \Phi(t) = J(tx + (1-t)y) (x - y),\]

<p>by the fundamental theorem of calculus, we then get that</p>

\[T(x) - T(y) = T(\Phi(1)) - T(\Phi(0)) = \int_{0}^1 \frac{\d}{\d t} T(\Phi(t)) \d t = \int_{0}^1  J(tx + (1-t)y) (x - y) \d t,\]

<p>and taking the \(L_p\) norm of both sides, by triangle inequality and submultiplicativity we get</p>

\[\begin{align*}
    \Vert T(x) - T(y) \Vert
    &amp;= \left \Vert \int_{0}^1  J(tx + (1-t)y) (x - y) \d t \right \Vert  \\
    &amp;\leq \int_{0}^1 \left \Vert  J(tx + (1-t)y) \right \Vert \d t \Vert x - y \Vert 
    \leq c \Vert x - y \Vert,
\end{align*}\]

<p>i.e., \(T\) is a contraction with contraction constant \(c\). \(\endproof\)</p>

<h2 id="conclusion">Conclusion</h2>
<p>In this note, we explored how some iterative numerical algorithms can be understood via fixed point iterations, the basic formulation of the Banach fixed point theorem and its proof, along with a practical sufficient condition for contractivity. This note, of course, only scratches the very top surface of the vast realm of fixed point theories, and we will explore a little more about its applications in the next and final part of this series of notes.</p>

<p><br /></p>

<h2 id="further-readings-and-references">Further readings and references</h2>

<ol>
  <li>Keith Conrad’s expository papers on Banach fixed point theorm <a href="https://kconrad.math.uconn.edu/blurbs/analysis/contraction.pdf">Part 1</a> <a href="https://kconrad.math.uconn.edu/blurbs/analysis/contraction2.pdf">Part 2</a></li>
  <li><a href="https://terpconnect.umd.edu/%7Epetersd/666/fixedpoint.pdf">Banach fixed point theorem in \(\mathbb{R}^n\)</a></li>
  <li><a href="https://en.wikipedia.org/wiki/Metric_space">Metric space</a></li>
</ol>

<p><em>Last updated: 2025-09-01 5:02 EST</em></p>]]></content><author><name>Liangjie (Jeffrey) Chen</name></author><category term="math" /><category term="algorithm" /><summary type="html"><![CDATA[Part II of a three-part note on how the common existence and uniqueness results in ODEs and MDPs boil down to the Banach fixed point theorem, which itself is based on the simple geometric series.]]></summary></entry><entry><title type="html">From geometric series to fixed-point iterations and their applications: Part I</title><link href="https://ljchen.ca/blurbs/2025/08/geo-series-fixed-point-1/" rel="alternate" type="text/html" title="From geometric series to fixed-point iterations and their applications: Part I" /><published>2025-08-21T00:00:00+00:00</published><updated>2025-08-21T00:00:00+00:00</updated><id>https://ljchen.ca/blurbs/2025/08/geo-series-fixed-point-1</id><content type="html" xml:base="https://ljchen.ca/blurbs/2025/08/geo-series-fixed-point-1/"><![CDATA[<p>Part I of a three-part note on how the common existence and uniqueness results in ODEs and MDPs boil down to the Banach fixed-point theorem, which itself is based on the simple geometric series.</p>

<p>Most people will probably learn about the geometric series at some point during high school, when its uses during this first encounter are usually elementary and application-oriented. For example, the geometric series is used in calculating compound interest, rate of return, perhaps some physics phenomenon, though it has far more uses than one might think. In this three-part note, we will see (1) how the geometric series can be extended beyond a series of real or complex numbers, and (2) the central role of geometric series in proving the Banach fixed-point theorem, and (3) the two standard applications of the Banach fixed-point theorem: the Picard–Lindelöf theorem, and convergence of value iteration in Markov decision processes.</p>

<!-- The geometric series and its extension: the Neumann series
----- -->

<p>To begin, we recall that a geometric series takes the following form: for some number \(k\) and  \(a\),</p>

\[\begin{equation}\label{eq:geo-series-real}
    k + ka + ka^2 + \dots = \sum_{i = 0}^{\infty} ka^i, 
\end{equation}\]

<p>is a geometric series with a common ratio \(a\). If \(a &lt; 1\), it is called the “decay rate” and the series converges to \(\frac{k}{1-a}\). One important extension of this idea is the power series, where the leading coefficient \(k\) is no longer uniform, but instead itself forms a sequence, i.e.,</p>

\[k_0 + k_1 a + k_2 a^2 + \dots = \sum_{i = 0}^{\infty} k_i a^i,\]

<p>which is important and a powerful tool in its own right, e.g., in the context of analysis. To stay focused on the purpose of this note, we instead look at the extension into geometric series of a <em>linear operator</em> \(T: V \goesto V\), where we take \(V\) to be a <a href="https://en.wikipedia.org/wiki/Cauchy_sequence#Completeness">complete</a>, <a href="https://en.wikipedia.org/wiki/Dimension_(vector_space)">finite-dimensional</a>, <a href="https://en.wikipedia.org/wiki/Normed_vector_space">normed vector space</a>, also known as a <em>Banach space</em>. The standard \(n\)-dimensional Euclidean space \(\mathbb{R}^n\) is one such example. We begin by taking \(k = 1\) and \(a &lt; 1\) in \eqref{eq:geo-series-real}, which along with the convergence result stated previously, becomes</p>

\[\begin{equation}\label{eq:geo-series-real-1}
    \sum_{i = 0}^{\infty} a^i = (1-a)^{-1}.
\end{equation}\]

<p>With the linear operator \(T: V \goesto V\), we can define the <em>Neumann series</em>:</p>

\[\begin{equation}\label{eq:neumann-series}
    \sum_{i = 0}^{\infty} T^i,
\end{equation}\]

<p>which is a series of <em>repeated application</em> of the operator \(T\). 
<!-- To make this notion concrete, let us 
see 
how the gradient descent algorithm on an objective function \\(f : \mathbb{R} \goesot \mathbb{R}\\) might fit into this framework. Let \\(x_0 \inR\\) be some fixed initial condition, then we get 
$$
    x_n = x_{n-1} - \eta f'(x_{n-1}) \define T(x_{n-1}). 
$$
That is, the gradient descent iterations can be viewed as the sequence 
$$
    x_1 \define T(x_0), \; x_2 \define T(x_1) = T^2(x_0), \;, \dots, x_n \define T(x_{n-1}) = \cdots = T^{n}(x_0).
$$
With the simple quadratic objective function \\(f(x) = \frac{1}{2}x^2\\), we have \\(f'(x) = x\\), so 
$$
    x_{n} = x_{n-1} - \eta x_{n-1} = (1 - \eta) x_{n-1} = (1-\eta)^2 x_{n-2} = \cdots (1-\eta)^n x_0, 
$$
and the step size \\(\eta\\) to ensure algorithm convergence can be naturally obtained via the geometric series convergence criterion.   -->
<!-- Back to the general Neumann series \eqref{eq:neumann-series}.  -->
Ideally, the series converges, but what’s more, it would be nice to arrive at an analogous result to \eqref{eq:geo-series-real-1}. That is, we need to answer the following question: under what condition on \(T\) do we have \eqref{eq:neumann-series-conv}?</p>

\[\begin{equation}\label{eq:neumann-series-conv}
    \sum_{i = 0}^{\infty} T^i =(I-T)^{-1}
\end{equation}\]

<p>The answer is, when the operator norm of \(T\), denoted by \(\Vert T \Vert\), satisfies \(\Vert T \Vert &lt; 1\). This condition is clearly analogous to the convergence criterion of geometric series, \(\vert a \vert &lt; 1 \). The Neumann series has important applications in the topic of <em>resolvent analysis</em>, a topic in complex analysis with deep connections to linear algebra, and by extension, classical control theory topics such as the frequency response of a linear time-invariant system. An excellent treatment on this topic is given in the reference section at the end. This extension from the (scalar) geometric series to the (vector/operator) Neumann series will be examined further in Part II, where we introduce the Banach fixed-point theorem and its proof.</p>

<p>In addition, since we are only working with finite-dimensional vector spaces, it is natural to consider the square matrix \(A\), which is the matrix representation of operator \(T\) in the (canonical) basis. The ties to linear algebra now becomes evident: if the induced matrix norm of \(A\), denoted by \(\Vert A \Vert\), satisfies \(\Vert A \Vert &lt; 1\), we can then write</p>

\[\sum_{i = 0}^{\infty} A^i =(I-A)^{-1},\]

<p>which is a series of matrix powers. Thus, when the series converges, a natural application of this result is in matrix inversion computation: let \(B \define I - A\), and suppose that \(\Vert I - B\Vert &lt; 1\). Then, to compute the inverse \(B^{-1}\), we can write</p>

\[B^{-1} = (I - A)^{-1} = \sum_{i = 0}^{\infty} A^i = \sum_{i = 0}^{\infty} (I-B)^i.\]

<p>In practice, we may truncate the series after sufficiently large number of terms. In addition, a common trick used in computing the inverse of a sum of two matrices, \((A + B)^{-1}\), when one of them is invertible, is as follows: suppose that \(A\) is invertible and \(\Vert BA \Vert &lt; 1\), then</p>

\[(A + B)^{-1} = ((I + BA)A)^{-1} = A^{-1}(I + BA)^{-1} = A^{-1} \sum_{i=0}^{\infty} (-BA)^{i},\]

<p>which is a particularly useful formula in cases of \(B\) being a small perturbation to the “nominal” \(A\).</p>

<p>To conclude this very brief note, we can see that the geometric series is much more than a tool to calculate compound interest in high school, and can in fact be generalized to other mathematical objects. In the next part, we will explore how it is used in the study of fixed-point iterations, which is ubiquitous in algorithm development and convergence analysis. We will then prove the Banach fixed-point theorem, which is an extremely simple yet profound result.</p>

<p><br /></p>

<h2 id="further-readings-and-references">Further readings and references</h2>
<ol>
  <li><a href="https://www.maths.usyd.edu.au/u/daners/publ/abstracts/linalg/linalg.pdf">Jordan decomposition theorem and Cayley-Hamilton theorem via resolvent analysis</a></li>
  <li><a href="https://en.wikipedia.org/wiki/Operator_norm">Operator norm</a> and <a href="https://en.wikipedia.org/wiki/Matrix_norm#Matrix_norms_induced_by_vector_norms">induced matrix norm</a></li>
</ol>

<p><em>Last updated: 2025-08-22 12:56 EST</em></p>]]></content><author><name>Liangjie (Jeffrey) Chen</name></author><category term="math" /><summary type="html"><![CDATA[Part I of a three-part note on how the common existence and uniqueness results in ODEs and MDPs boil down to the Banach fixed-point theorem, which itself is based on the simple geometric series.]]></summary></entry><entry><title type="html">Equivalent formulations of semidefinite programs using linear matrix inequality and trace</title><link href="https://ljchen.ca/blurbs/2021/05/sdp/" rel="alternate" type="text/html" title="Equivalent formulations of semidefinite programs using linear matrix inequality and trace" /><published>2021-05-02T00:00:00+00:00</published><updated>2021-05-02T00:00:00+00:00</updated><id>https://ljchen.ca/blurbs/2021/05/sdp</id><content type="html" xml:base="https://ljchen.ca/blurbs/2021/05/sdp/"><![CDATA[<p>A semidefinite program written using the linear matrix inequality can be equivalently written using the trace of a matrix.</p>

<p><em>Acknowledgement: the content of this post originates from a homework question in Prof. Stark Draper’s course, ECE1505 Convex Optimization, at the University of Toronto.</em></p>

<h2 id="semidefinite-program-and-linear-matrix-inequality">Semidefinite program and linear matrix inequality</h2>
<p>Semidefinite programs (SDPs) are a specific type of <em>conic form programs</em>, which is a subclass of convex optimization problems with respect to the <a href="https://en.wikipedia.org/wiki/Loewner_order">Lowener order</a>, that is, the cone of (symmetric) positive semidefinite matrices denoted by \(\mathbb{S}_{+}^{n}\). SDPs are typically formulated as</p>

\[\begin{equation}\label{eq:sdp-lmi} 
\begin{array}{cl}
    \minimize{x \in \mathbb{R}^n} &amp; \transpose{c}x  \\ 
    \st &amp; F_0 + x_1F_1 + \cdots + x_n F_n \preceq \zero \\
    &amp; Gx = h
\end{array}  
\end{equation}\]

<p>where \(c \inR^n, G \inR^{p\times n}, h \inR^p\), and each \(F_i\) is an \(m \times m\) real symmetric matrix, denoted by \(F_i \inS^m\). The inequality constraint is known as a <em>linear matrix inequality</em> (LMI), which is a generalized inequality with respect to the partial order defined by the convex cone of symmetric positive definite matrices. In addition, while the decision variable in the SDP above is the vector \(x \inR^n\), they can often appear as matrices (not even necessarily symmetric matrices) in an SDP. For example, for \(i \in \{1, \dots, N\}\), let \(A_i \inR^{n \times n}, B_i \inR^{n \times n}, C_i \inR^{n \times m}\) be constant coefficient matrices, and let \(X \inS^n_{++}\) and \(Y \inR^{m \times n}\) be unknown decision variables, then</p>

\[F(X,Y) \define F_0 + \sum_{i = 1}^N A_i X B_i + \transpose{B}_i X \transpose{A}_i + C_i Y + \transpose{Y} \transpose{C}_i \succeq \zero\]

<p>is a perfectly valid LMI in \(X\) and \(Y\). This form of LMIs often appears in SDPs used in, e.g., systems control theory.</p>

<h2 id="equivalent-sdp-formulation-using-trace-of-a-square-matrix">Equivalent SDP formulation using trace of a square matrix</h2>
<p>The goal of this post is to show that, another SDP formulation, now written with the trace of a matrix, denoted by \(\trace(\cdot)\),</p>

\[\begin{equation}\label{eq:sdp-trace}
\begin{array}{cl}
    \minimize{X \in \mathbb{S}^{\bullet}_+} &amp; \trace(CX)  \\ 
    \st &amp; \trace(A_k X) = b_k, \quad k \in \{1, \dots, q\}\\
    &amp; X \succeq \zero
\end{array} 
\end{equation}\]

<p>is in fact equivalent to \eqref{eq:sdp-lmi}, and we show how to establish this equivalence by appropriately constructing the decision variable \(X\), and the coefficients \(C, A_k\), and \(b_k\). We leave the dimension of the new decision variable \(X\) vague on purpose for now, as the construction below will clarify it. Now, let us recall that the <em>trace</em> of a square matrix is defined as the sum of its diagonal elements, and is connected to the standard Frobenius inner product on \(\mathbb{R}^{m \times n}\) via</p>

\[\trace(\transpose{X}Y) = \trace(X\transpose{Y}) = \langle X, Y \rangle = \sum_{i=1}^{m}\sum_{j=1}^n X_{ij}Y_{ij}.\]

<p>There are many additional properties of the matrix trace and its generalization that we will not discuss here, and we include some relevant references at the end of this post.</p>

<p>We are now ready to convert \eqref{eq:sdp-lmi} into \eqref{eq:sdp-trace}. First, from the left-hand side of the LMI in \eqref{eq:sdp-lmi}, define a slack variable from the left-hand side of the LMI in \eqref{eq:sdp-lmi}:</p>

\[\begin{equation}\label{eq:X-tilde}
\tilde{X} \define -F_0 - \sum_{i=1}^n x_i F_i.
\end{equation}\]

<p>We further decompose the decision variable \(x\) element-wise by its nonnegative part and its nonpositive part, i.e., \(x = x^+ - x^-\), in which both \(x^+\) and \(x^-\) are non-negative element-wise. (Of course, such decomposition is not unique, and we just pick one). We also denote the diagonal matrix formed by placing the elements of a vector \(v\) on its main diagonal as \(\diag(v)\). We claim that the block diagonal matrix</p>

\[X \define \begin{bmatrix}
    -\tilde{X} &amp; \\
    &amp; \diag(x^+) \\
    &amp; &amp; \diag(x^-)
\end{bmatrix} \inR^{(m+2n) \times (m+2n)}\]

<p>is exactly the decision variable in \eqref{eq:sdp-trace}. As a sanity check, since \(F(x) \preceq \zero\) in \eqref{eq:sdp-lmi}, the new decision variable \(X\) above is positive semidefinite, and should be a sparse matrix provided that \(m\) is not significantly larger than \(n\). 
Analogous to the decomposition of \(x\), we can decompose the cost vector \(c\) in \eqref{eq:sdp-lmi} as \(c = c^+ - c^-\), in which both \(c^+\) and \(c^-\) are non-negative element-wise, and construct the diagonal cost matrix</p>

\[C \define \begin{bmatrix}
    \zero_{m\times m} &amp; \\
    &amp; \diag(c^+) \\
    &amp; &amp; \diag(c^-)
\end{bmatrix} \inR^{(m+2n) \times (m+2n)}\]

<p>which satisfies \(\trace(CX) = \transpose{c}x\). We can now compare against \eqref{eq:sdp-lmi}, and see that we need to handle the \(q\) equality constraints in \eqref{eq:sdp-trace}, which has two sources. The first one is due to the definition of the slack variable \(\tilde{X}\) in \eqref{eq:X-tilde}, which must be satisfied element-wise. Thus, given the coefficient matrices \(F_i \inS^{m}\), we need to introduce (at most) \(\frac{m(m+1)}{2}\) equality constraints as follows: define the constant matrices</p>

\[\begin{equation} \label{eq:F-tilde}
\tilde{F}_{j\ell} \define \begin{bmatrix}
    \Delta_{\ell j} &amp; \\
    &amp; \diag(F_{i,j \ell}) \\
    &amp; &amp; -\diag(F_{i,j \ell}) 
\end{bmatrix} \inR^{(m+2n)\times (m+2n)}, 
\end{equation}\]

<p>where \(\Delta_{\ell j} \inR^{m \times m}\) is the indicator matrix with \(1\) at the \(\ell, j\)-th element and zero everywhere else, and</p>

\[\diag(F_{i,j \ell}) \define \begin{bmatrix} 
        (F_1)_{j\ell} &amp; \\
        &amp; (F_2)_{j\ell} &amp; \\
        &amp; &amp; \ddots &amp; \\
        &amp; &amp; &amp; (F_n)_{j\ell} &amp; \\
    \end{bmatrix} \inR^{n \times n}\]

<p>is a diagonal matrix with the \(j,\ell\)-th elements of matrices \(F_1, \dots, F_n\) on its main diagonal. Thus, we can verify that</p>

\[\begin{align}
    \trace(\tilde{F}_{j\ell} X) &amp;= \trace(\Delta_{\ell j} \tilde{X}) + \sum_{i=1}^n x_i^+ (F_i)_{j\ell} - \sum_{i=1}^n x_i^- (F_i)_{j\ell} \nonumber \\
                        &amp;= \tilde{X}_{j\ell} + \sum_{i=1}^n(x_i^+  - x_i^-) (F_i)_{j\ell} \nonumber \\
                        &amp;= -(F_0)_{j\ell} - \sum_{i=1}^n x_i (F_i)_{j\ell} +  \sum_{i=1}^n x_i (F_i)_{j\ell} \nonumber \\
                        &amp;= -(F_0)_{j\ell}  \label{eq:eq-constraint-lmi}
\end{align}\]

<p>There are a total of \(\frac{m(m+1)}{2}\) constraints like \eqref{eq:eq-constraint-lmi}, and they form the first part of the equality trace constraints in \eqref{eq:sdp-trace}. The second and the last part comes from \(Gx = h\) in \eqref{eq:sdp-lmi}, which can be transformed by constructing</p>

\[\begin{equation} \label{eq:G-tilde}
    \tilde{G}_{i} \define \begin{bmatrix}
        \zero_{m \times m} &amp; \\
        &amp; \diag(\transpose{\e}_i G) \\
        &amp; &amp; -\diag(\transpose{\e}_i G) 
    \end{bmatrix} \inR^{(m+2n)\times (m+2n)}
\end{equation}\]

<p>in which \(e_i \inR^p\) is the \(i\)-th unit vector, thus \(\transpose{\e}_i G \inR^n\) selects the \(i\)-th row of \(G\). For \(i \in \{1, \dots, p\}\), \(\transpose{\e}_i G x = h_i\), so \(Gx = h\) in \eqref{eq:sdp-lmi} is equivalent to the \(p\) trace constraints</p>

\[\tilde{G}_i X = h_i.\]

<p>Thus, from \eqref{eq:sdp-trace}, we have a total of</p>

\[q = \frac{m(m+1)}{2} + p\]

<p>trace equality constraints of the form \(\trace(A_kX) = b_k\), in which the new coefficient matrices \(A_k\) correspond to \(\tilde{F}_{j\ell}\) and \(\tilde{G}_i\) constructed in \eqref{eq:F-tilde} and \eqref{eq:G-tilde}, respectively, while the coefficients \(b_k\) correspond to \(-(F_0)_{j\ell}\) and \(h_i\), respectively.</p>

<p><br /></p>

<h2 id="further-readings-and-references">Further readings and references</h2>
<ol>
  <li>General introduction and basic examples of SDPs: Chapter 4.6.2 of <a href="https://web.stanford.edu/~boyd/cvxbook/bv_cvxbook.pdf">Convex Optimization</a></li>
  <li><a href="https://doi.org/10.1137/1038003">SIAM survey</a> of SDPs and numerical solution methods</li>
  <li>LMIs in systems and control theory <a href="https://web.stanford.edu/~boyd/lmibook/lmibook.pdf">book</a></li>
  <li>Further trace properties and generalizations: Chapter 8.D of <a href="https://linear.axler.net/LADR4e.pdf">Linear Algebra Done Right</a></li>
</ol>

<p><em>Last updated: 2025-07-22 15:32 EST</em></p>]]></content><author><name>Liangjie (Jeffrey) Chen</name></author><category term="optimization" /><category term="coursework" /><summary type="html"><![CDATA[A semidefinite program written using the linear matrix inequality can be equivalently written using the trace of a matrix.]]></summary></entry><entry><title type="html">Fréchet derivative and common formulas</title><link href="https://ljchen.ca/blurbs/2021/04/vec-derivative/" rel="alternate" type="text/html" title="Fréchet derivative and common formulas" /><published>2021-04-26T00:00:00+00:00</published><updated>2021-04-26T00:00:00+00:00</updated><id>https://ljchen.ca/blurbs/2021/04/vector-derivative</id><content type="html" xml:base="https://ljchen.ca/blurbs/2021/04/vec-derivative/"><![CDATA[<p>Some basic Fréchet derivative formulas one often encounters in optimization problems.</p>

<p><em>Acknowledgement: the content of this post originates from Appendix A.4 of Convex Optimization by Boyd &amp; Vandenberghe, and the Introduction to Nonlinear Systems (ECE1647) lecture notes by Prof. Manfredi Maggiore at University of Toronto.</em></p>

<h2 id="definitions-and-properties">Definitions and properties</h2>

<p>In this brief note, we introduce the <em>Fréchet derivative</em> for vector valued functions in finite-dimensional Banach spaces, and derive some formulas commonly seen for practical applications in optimization and control theory. We focus on functions</p>

\[f : X \goesto Y,\]

<p>where \(X \subset \mathbb{R}^n\) and \(Y \subset \mathbb{R}^m\) are <a href="https://en.wikipedia.org/wiki/Domain_(mathematical_analysis)">domains</a> (open and connected sets), and \(n, m \geq 1\) are integers. Note that we restrict the attention to such domains to have better control over boundary and local behaviors in the ensuing definitions. In particular, since most objective functions we will use are real-valued, i.e., \(m = 1\), most formulas in this note pertains to this case.</p>

<p>To begin, we recall the basic derivative definition when \(n = m = 1\), which we would see in the first calculus course: a function \(f : \mathbb{R} \goesto \mathbb{R}\) is differentiable at \(x \inR\) if the limit</p>

\[\begin{equation}\label{eq:diff-scalar}
    \lim_{h \goesto 0} \frac{f(x + h) - f(x)}{h}
\end{equation}\]

<p>exists. The limit is called the <em>derivative of \(f\) at \(x\)</em>, and denoted by \(f’(x)\). We are taught that the derivative acts like a linearization (or linear approximation of \(f\)) at the point \(x\), which provides many convenient tools for analysis and computation. Equivalently, we can write \eqref{eq:diff-scalar} as</p>

\[\begin{equation}\label{eq:diff-scalar-1}
    \lim_{h \goesto 0} \frac{\vert f(x + h) - f(x) - f'(x)h \vert}{\vert h\vert} = 0.
\end{equation}\]

<p>To generalize this definition to vector- and matrix-valued functions, we use the <em>Fréchet derivative</em>. (Note: we will only discuss the case of <em>vector-</em> valued functions, since matrix-valued functions can be equivalently understood via the linear, bijective <em><a href="https://en.wikipedia.org/wiki/Vectorization_(mathematics)">vecorization</a></em> operation, even though it is only an algebraic “convenience” and does not reflect many deeper geometric meanings of matrix-valued functions). We say \(f: X \goesto Y\) is (Fréchet) differentiable at \(x \in X\) if there exists a linear operator \(Df : \mathbb{R}^n \goesto \mathbb{R}^m\), such that</p>

\[\begin{equation}\label{eq:diff-frechet}
    \lim_{h \goesto \zero} \frac{\Vert f(x + h) - f(x) - Df(x)h \Vert}{\Vert h\Vert} = \zero. 
\end{equation}\]

<p>The Fréchet derivative of \(f\) at a point \(x \in X\) is denoted by \Df(x)\, and is unique if it exists. We can observe the similarity between \eqref{eq:diff-frechet} and \eqref{eq:diff-scalar-1}. Note that the norms in \eqref{eq:diff-frechet} are understood to be \(L_p\) norms consistent with the dimensions \(n,m\). If \(f\) is differentiable at <em>every</em> \(x \in X\), then we say \(f\) is <em>differentiable</em>. Furthremore, if \(f\) is differentiable, and the map \(x \mapsto Df\) from \(X\) to \(\mathbb{R}^{m \times n}\) is continuous, then we say \(f\) is <em>continuously differentiable</em>, and write \(f\) is \(\mathcal{C}^1\). Under the standard bases of \(\mathbb{R}^n\) and \(\mathbb{R}^m\), the matrix representation of \(Df\) is the <em>Jacobian matrix</em>. Let \(\{\e_1, \dots, \e_n\}\) be the set of standard basis vectors in \(\mathbb{R}^n\), we define</p>

\[\frac{\partial f_{i}}{\partial x_j}(x) = \lim_{t \goesto 0} \frac{f(x + t \e_j) - f(x)}{t},\]

<p>as the \(j\)-th <em>partial derivative</em> of \(f\) at \(x\), if the limit exists. As a simple example, when \(f : \mathbb{R}^n \goesto \mathbb{R}\), we get</p>

\[Df(x) = \begin{bmatrix}
        
        \dfrac{\partial f}{\partial x_1} &amp; \dfrac{\partial f}{\partial x_2} \dots \dfrac{\partial f}{\partial x_n}, 
    \end{bmatrix}\]

<p>and its transpose is the (more) commonly seen <em>gradient</em>, denoted by \(\nabla f(x)\).</p>

<p>The classic chain rule can also be extended to vector-valued functions: let \(X,Y\) and \(Z\) be domains of \(\mathbb{R}^n\), \(\mathbb{R}^m\), and \(\mathbb{R}^p\), respectively, and let \(f : Y \goesto Z\), \(g : X \goesto Y\) be differentiable. Then, the composite function \(f \odot g : X \goesto Z\) is differentiable and</p>

\[D(f \odot g)(x) = Df(g(x))Dg(x).\]

<p>We can further define the second Fréchet derivative, \(D(Df)\), and even higher order derivatives, provided \(f\) is sufficiently smooth. To be rigorous, we would need to properly define the spaces (bounded multilinear maps) that we work with. However, these definitions get quite messy with the introduction of required advanced tools, which are often unnecessary for practical purposes (see Reference 1 for a better treatment). Instead, we focus on the case of real-valued functions \(f : \mathbb{R}^n \goesto \mathbb{R}\), whose second derivative, under the standard basis, is commonly given by the <em>Hessian matrix</em>:</p>

\[\nabla^2 f(x) = D(\nabla f)(x) = \begin{bmatrix}
        \dfrac{\partial^2 f(x)}{\partial x_1\partial x_1} &amp; \cdots &amp; \dfrac{\partial f^2(x)}{\partial x_1 \partial x_n} \\
        \vdots &amp; \ddots &amp; \vdots \\
        \dfrac{\partial^2 f(x)}{\partial x_n \partial x_1} &amp; \cdots &amp; \dfrac{\partial^2 f(x)}{\partial x_n \partial x_n}
\end{bmatrix}.\]

<p>If \(f\) is <em>twice continuously differentiable</em>, i.e., \(f\) is \(\mathcal{C}^2\), then its Hessian is symmetric.</p>

<h2 id="some-common-formulas">Some common formulas</h2>

<p>Next, we go through some (simple but tedious) example derivative calculations. When I took my first course in optimization, I found these steps helpful to convince myself of the results, but again, they are quite tedious.</p>

<ol>
  <li>
    <p>Take \(f(x) = \transpose{a} x\) for some known vector \(a \inR^{n}\). Then, \(Df(x) = \transpose{a}\) and \(\nabla f(x) = a\).</p>

    <p><em>Proof</em>.</p>

\[D \left(\transpose{a}x\right) = 
     \begin{bmatrix}
         \dfrac{\partial}{\partial x_1}\sum_{i=1}^n a_ix_i &amp;
         \cdots &amp;
         \dfrac{\partial}{\partial x_n}\sum_{i=1}^n a_ix_i
     \end{bmatrix} = 
     \begin{bmatrix}
         a_1 &amp; \cdots &amp; a_n
     \end{bmatrix}
     = \transpose{a}.\]
  </li>
  <li>
    <p>Take \(f(x) = Ax\) for some known matrix \(A \inR^{m \times n}\). Then, \(Df(x) = A\).</p>

    <p><em>Proof</em>.</p>

\[D (Ax) = D \left(\begin{bmatrix}
     a_{11} &amp; \cdots &amp; a_{1n} \\ 
     \vdots &amp; \ddots &amp; \vdots \\
     a_{n1} &amp; \cdots &amp; a_{nn}
 \end{bmatrix}\begin{bmatrix}
     x_1 \\ \vdots \\ x_n
 \end{bmatrix} \right)
 = \begin{bmatrix}
     D\left( \sum_{i=1}^n a_{1i}x_i \right)\\
     \vdots \\
     D\left(\sum_{i=1}^n a_{ni}x_i \right)
 \end{bmatrix}.\]

    <p>From the previous identity, \(D \left(\sum_{i=1}^n a_{ji}x_i\right)\) for \(j \in \{1,…,n\}\) results in the row vector</p>

\[\begin{bmatrix}
     a_{j1} &amp; \cdots &amp; a_{jn}
 \end{bmatrix},\]

    <p>which is precisely the \(j\)-th row of the matrix \(A\). Therefore, \(D(Ax) = A\).</p>
  </li>
  <li>
    <p>For a matrix \(A \inS^n\), i.e., \(A = \transpose{A}\), we have that \(D \left(\transpose{x}Ax\right) = 2Ax\).</p>

    <p><em>Proof</em>. 
 If we treat \(f(x) = \transpose{x}Ax\), evidently \(f\) is real valued, so we want to find</p>

\[\begin{bmatrix}\dfrac{\partial f(x)}{\partial x_1} &amp; \cdots &amp; \dfrac{\partial f(x)}{\partial x_n}\end{bmatrix}.\]

    <p>Expanding \(f\), for \(k \in \{1,…,n\}\), we have</p>

\[\begin{align*}
     \transpose{x}A{x} &amp;= \sum_{i=1}^n x_i \sum_{j=1}^n a_{ij}x_j \\
     &amp;= x_k\sum_{j=1}^na_{kj}x_j + \sum_{i\neq k} x_i\sum_{j=1}^n a_{ij}x_j \\
     &amp;= x_k\left(a_{kk}x_k + \sum_{j\neq k} a_{kj}x_j\right) + \sum_{i\neq k} \left( x_ia_{ik}x_k + x_i \sum_{j\neq k} a_{ij}x_j\right) \\
     &amp;= a_{kk}x_k^2 + \left(\sum_{j\neq k} a_{kj}x_j\right)x_k + \left(\sum_{i\neq k} a_{ik}x_i\right)x_k + \sum_{i\neq k}\sum_{j\neq k} a_{ij}x_jx_i.
 \end{align*}\]

    <p>Take the derivative of each term above with respect to \(x_k\), and we get</p>

\[\begin{align*}
     \frac{\partial}{\partial x_k} \transpose{x}Ax = 2a_{kk}x_k + \sum_{j\neq k} a_{kj}x_j + \sum_{i\neq k} a_{ik}x_i 
     = 2a_{kk}x_k + 2\sum_{j\neq k} a_{kj}x_j = 2\sum_{j=1}^n a_{kj}x_j,
 \end{align*}\]

    <p>which is twice the inner product between the \(k\)-th row of \(A\) and \(x\). We can combine the two sums in the second step above since \(A = \transpose{A}\), so \(a_{kj} = a_{ik}\).  Therefore, the full derivative \(D\left(\transpose{x}Ax\right)\) is \(2Ax\). Note that, typically we consider the function \(\frac{1}{2}\transpose{x}Ax\) instead, where \(A \inS^n\) cancels out the constant 2 in the result, since optimizers are not impacted by such constant factors.</p>
  </li>
  <li>
    <p>It follows from the formulas above that \(\nabla^2 \left( \transpose{x}Ax\right) = 2A\).</p>
  </li>
</ol>

<p><br /></p>

<h2 id="further-readings-and-references">Further readings and references</h2>
<ol>
  <li>Fréchet derivative in <a href="https://desvl.xyz/2020/07/31/frechet-derivative/">general Banach spaces</a></li>
</ol>

<p><em>Last updated: 2025-09-03 15:32 EST</em></p>]]></content><author><name>Liangjie (Jeffrey) Chen</name></author><category term="optimization" /><category term="math" /><summary type="html"><![CDATA[Some basic Fréchet derivative formulas one often encounters in optimization problems.]]></summary></entry></feed>